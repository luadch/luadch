local adclib = use "adclib"
local types = use "types"

return function( )

    --adclib.hash{ }
    --adclib.hashpas{ }
    --adclib.hasholdpas{ }
    --adclib.escape{ }
    --adclib.unescape{ }
    --adclib.isutf8{ }

    types.check( "הההה", "adccmd" )
end
